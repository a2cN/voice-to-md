{
  "name": "voice-to-md",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "build": "tsc && cp index.html appsscript.json dist/",
    "deploy": "npm run build && clasp push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/google-apps-script": "^2.0.8",
    "typescript": "^5.9.3"
  }
}
